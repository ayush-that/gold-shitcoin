[phases.setup]
nixPkgs = ["nodejs_20", "corepack", "curl"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.11.0 --activate",
  "pnpm install --prod"
]

[start]
cmd = "node server.js"
